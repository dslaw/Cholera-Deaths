#ifndef __UTILS_H
#define __UTILS_H

#include <fstream>
#include <sstream>
#include <stdexcept>
#include <string>
#include <vector>


std::vector<std::size_t> read_counts(std::string filename);

#endif
